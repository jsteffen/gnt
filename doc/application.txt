2. Application/testing phase

load model and alphabet, word vectors and suffix and shape factories; the later actually only for the functions

given a sentence of tokens crate a list o pairs of (word, pos) where pos here is actually empty or filled in case of evaluation

create feature representation of test sentence analog to training

call predictor

re-map found solution -> label
